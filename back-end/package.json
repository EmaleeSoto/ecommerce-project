{
  "name": "back-end",
  "version": "1.0.0",
  "description": "Ecommerce Project api server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "psql -U postgres -f db/schema.sql && psql -U postgres -f db/seed.sql"
  },
  "author": "Emalee Soto",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg-promise": "^11.0.2",
    "stripe": "^11.9.1"
  }
}
